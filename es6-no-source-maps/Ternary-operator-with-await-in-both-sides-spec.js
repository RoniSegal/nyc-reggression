const {run} = require('./Ternary-operator-with-await-in-both-sides');
 describe('es6-no-source-maps-Ternary-operator-with-await-in-both-sides', () => {
    it('test',async () => {
        await run()
    })
 })
